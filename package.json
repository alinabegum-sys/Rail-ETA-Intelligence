{
  "name": "rail-eta-intelligence",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "test": "node -e \"console.log('Build verified')\""
  }
}
